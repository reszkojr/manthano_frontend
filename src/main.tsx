import React from 'react';
import ReactDOM from 'react-dom/client';

import Manthano from './Manthano.tsx';

ReactDOM.createRoot(document.getElementById('root')!).render(
	<React.StrictMode>
		<Manthano />
	</React.StrictMode>
);
